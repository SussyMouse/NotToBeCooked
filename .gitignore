# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# env files (can opt-in for committing if needed)
.env

# turbo
.turbo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# typescript
*.tsbuildinfo

# python
__pycache__/
*.py[cod]
.venv
venv
.pytest_cache
.ruff_cache

# uploaded files (local object store stand-in)
apps/api/storage/

# personal type-checker / editor configs. pyrefly.toml is not a project
# dependency -- it appears in no pyproject.toml, package.json or uv.lock -- and
# the one it replaced hard-coded `apps/api/.venv/bin/python`, which does not
# exist on Windows, so every Windows checkout of dev lit up red.
pyrefly.toml
