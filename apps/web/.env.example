# Base URL of the FastAPI backend, read by ApiClient.resolveBaseUrl().
# Unset falls back to http://localhost:8000, which is correct for local dev
# and wrong everywhere else — set it explicitly when deploying.
VITE_API_URL=http://localhost:8000
